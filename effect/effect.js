function effectdesc()
{
    if (document.all.framedesc != null)
    {
        document.all.framedesc.innerHTML = "エフェクト全体の時間をフレーム数で指定";
    }
    
    if (document.all.colordesc != null)
    {
        document.all.colordesc.innerHTML = "色をRGBでで指定";
    }
}
